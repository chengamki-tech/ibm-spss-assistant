# 各类分析结果解读模板

## t 检验结果解读

### 输出表格关键列
- **Levene's Test for Equality of Variances**: 判断方差齐性
  - Sig. > .05 → 方差齐性，读"Equal variances assumed"行
  - Sig. ≤ .05 → 方差不齐，读"Equal variances not assumed"行
- **t**: t 统计量
- **df**: 自由度
- **Sig. (2-tailed)**: 双尾 p 值
- **Mean Difference**: 均值差
- **95% Confidence Interval of the Difference**: 均值差的 95% CI

### 解读模板

```
Levene 检验结果:
- F = [值], p = [值]
- 判断: [p > .05, 方差齐性满足 / p ≤ .05, 方差不齐]

t 检验结果 (选择对应行):
- t([df]) = [t值], p = [p值]
- 均值差 = [差值]
- 95% CI [下限, 上限]

解读:
[组A] 的 [变量] 得分 (M = [均值], SD = [标准差]) [显著高于/低于/与]
[组B] (M = [均值], SD = [标准差])。

效应量:
- Cohen's d = [值] → [小/中/大]效应
- d 计算: 均值差 / 合并标准差

结论:
[用通俗语言说明差异的实际意义]
```

---

## 配对样本 t 检验解读

```
配对样本 t 检验结果:
- t([df]) = [t值], p = [p值]
- 均值差 = [差值], SD_diff = [差值标准差]
- 95% CI [下限, 上限]

解读:
被试在 [后测/条件B] 的 [变量] 得分 (M = [均值], SD = [标准差])
显著 [高于/低于] [前测/条件A] (M = [均值], SD = [标准差])。

效应量:
- Cohen's d = [值] (均值差 / 差值标准差)
```

---

## ANOVA 结果解读

### 输出表格关键内容
- **Test of Homogeneity of Variances**: Levene 方差齐性检验
- **ANOVA 表**: F 值、df、显著性
- **Post Hoc 多重比较**: 各组两两比较
- **Descriptives**: 各组描述统计

### 解读模板

```
前提检验:
- 方差齐性: Levene's F([df1], [df2]) = [值], p = [值]
  → [满足/不满足] 方差齐性假设

ANOVA 结果:
- F([df1], [df2]) = [F值], p = [p值]
- 效应量: η² = [值] → [小/中/大]效应

判断: [p < .05, 组间差异显著 / p ≥ .05, 组间差异不显著]

事后检验 (若显著):
- [组A] vs [组B]: 均值差 = [值], p = [值] → [显著/不显著]
- [组A] vs [组C]: 均值差 = [值], p = [值] → [显著/不显著]
- [组B] vs [组C]: 均值差 = [值], p = [值] → [显著/不显著]

结论:
[哪些组之间存在显著差异，差异方向是什么]
```

---

## 重复测量 ANOVA 解读

```
前提检验:
- Mauchly 球形检验: χ²([df]) = [值], p = [值]
  → [满足球形假设 / 不满足，需看 Greenhouse-Geisser 校正]

重复测量 ANOVA 结果:
- [球形假设满足: F([df1], [df2]) = [值], p = [值], 偏 η² = [值]]
- [不满足: F([ε×df1], [ε×df2]) = [值], p = [值], 偏 η² = [值]]
  (ε = [Greenhouse-Geisser ε值])

事后比较 (Bonferroni 校正):
- [时间点1] vs [时间点2]: 均值差 = [值], p = [值]
- [时间点1] vs [时间点3]: 均值差 = [值], p = [值]
- [时间点2] vs [时间点3]: 均值差 = [值], p = [值]
```

---

## 双因素 ANOVA 解读

```
方差齐性: Levene's F = [值], p = [值] → [满足/不满足]

主效应:
- [因素A] 的主效应: F([df1], [df2]) = [值], p = [值], η² = [值] → [显著/不显著]
- [因素B] 的主效应: F([df1], [df2]) = [值], p = [值], η² = [值] → [显著/不显著]

交互效应:
- [因素A] × [因素B]: F([df1], [df2]) = [值], p = [值], η² = [值] → [显著/不显著]

[交互效应显著时]:
简单效应分析表明:
- 在 [因素B=水平1] 时，[因素A] 的效应 [显著/不显著] (F = [值], p = [值])
- 在 [因素B=水平2] 时，[因素A] 的效应 [显著/不显著] (F = [值], p = [值])
```

---

## 相关分析解读

### Pearson 相关
```
相关系数: r([df]) = [r值], p = [p值]

方向: [正相关 / 负相关 / 无相关]
强度: [|r| < .10 几乎无 / .10-.30 弱 / .30-.50 中等 / .50+ 强]

效应量: r² = [值] → [变量A] 可解释 [变量B] [百分比]% 的方差

解读:
[变量A] 与 [变量B] 之间存在显著的 [正/负] 相关，
即 [变量A] 越高，[变量B] 越 [高/低]。
```

### Spearman 相关
```
Spearman 秩相关: rs = [值], p = [p值]

解读:
[变量A] 与 [变量B] 之间存在显著的 [正/负] 秩相关。
[用通俗语言: 排名的规律，而非具体数值的关系]
```

---

## 回归分析解读

### 线性回归
```
模型拟合:
- R² = [值], 调整 R² = [值]
- 模型可解释因变量 [百分比]% 的方差
- F([df1], [df2]) = [F值], p = [p值] → 模型 [整体显著/不显著]

各自变量贡献:
| 变量 | B | β | t | p | VIF | 解读 |
|------|---|---|---|---|-----|------|
| [var1] | [值] | [值] | [值] | [值] | [值] | [显著/不显著] |
| [var2] | [值] | [值] | [值] | [值] | [值] | [显著/不显著] |

多重共线性:
- VIF 均 < 10 → [无/存在] 多重共线性问题

残差检验:
- Durbin-Watson = [值] → [1.5-2.5 满足独立性]
- 残差图 → [是否满足等方差和正态假设]
```

### 层次回归
```
层次回归结果:
- Step 1 (控制变量): R² = [值], F = [值], p = [值]
- Step 2 (+ 核心变量): R² = [值], F = [值], p = [值]
- ΔR² = [值], ΔF([df1], [df2]) = [值], p = [值]

解读:
在控制 [变量A]、[变量B] 后，[核心变量] 仍对 [因变量]
有显著的 [正向/负向] 预测作用，解释了额外 [百分比]% 的方差。
```

---

## 逻辑回归解读

```
模型拟合:
- -2LL = [值]
- Omnibus χ²([df]) = [值], p = [值] → 模型 [有效/无效]
- Hosmer-Lemeshow χ² = [值], p = [值] → [拟合良好/不佳]
- Nagelkerke R² = [值]
- 预测正确率: [百分比]%

各自变量结果:
| 变量 | B | Wald | df | p | OR | 95% CI |
|------|---|------|---|---|---|---|
| [var1] | [值] | [值] | [值] | [值] | [值] | [下限, 上限] |

OR 解读:
- OR = 1.0: 无影响
- OR > 1.0: 每增加 1 单位，优势增加 (OR-1)×100%
- OR < 1.0: 每增加 1 单位，优势减少 (1-OR)×100%
- CI 包含 1.0 → 效应不显著
```

---

## 卡方检验解读

```
前提检查:
- 期望频率 < 5 的格子比例: [百分比]
- [可以使用卡方检验 / 需要用 Fisher 精确检验]

卡方检验结果:
- χ²([df], N = [样本量]) = [χ²值], p = [p值]
- Cramer's V = [值] → [小/中/大]效应

交叉表百分比 (看行百分比):
| | [列1] | [列2] | 合计 |
|---|---|---|---|
| [行1] | n (xx%) | n (xx%) | 100% |
| [行2] | n (xx%) | n (xx%) | 100% |

解读:
[变量A] 与 [变量B] 之间存在显著关联。
具体而言，[行1] 中 [列1] 的比例 (xx%) 显著 [高于/低于] [行2] (xx%)。
```

---

## 因子分析解读

```
适用性检验:
- KMO = [值] → [>.9 极好 / .8-.9 良好 / .7-.8 一般 / .6-.7 差 / <.6 不适合]
- Bartlett 球形检验: χ² = [值], df = [值], p = [值]

因子提取:
- 提取方法: 主成分分析
- 旋转方法: [Varimax 正交 / Promax 斜交]
- 特征值 > 1 的因子数: [N]
- 累计方差解释率: [百分比]%

旋转后因子载荷 (|载荷| > .40):
| 题项 | 因子1 | 因子2 | 因子3 |
|------|-------|-------|-------|
| item1 | .82 | | |
| item2 | .75 | | |

因子命名建议:
- 因子1: [根据题项内容命名]
- 因子2: [根据题项内容命名]
```

---

## 信度分析解读

```
整体信度:
- Cronbach's α = [值]
- 判断: [>.9 极好 / .8-.9 良好 / .7-.8 可接受 / .6-.7 勉强 / <.6 不可接受]

各题项分析:
| 题项 | 删除后α | CITC | 判断 |
|------|---------|------|------|
| item1 | .xx | .xx | [保留/考虑删除] |
| item2 | .xx | .xx | [保留/考虑删除] |

CITC < .30 或 删除后α 明显上升 → 建议删除
```

---

## 中介效应解读

```
中介效应检验结果:

(1) 总效应 (c path): [自变量] → [因变量]
    B = [值], SE = [值], p = [值] → [显著/不显著]

(2) 路径 a: [自变量] → [中介变量]
    B = [值], SE = [值], p = [值] → [显著/不显著]

(3) 路径 b: [中介变量] → [因变量] (控制 [自变量])
    B = [值], SE = [值], p = [值] → [显著/不显著]

(4) 间接效应 (a×b):
    Effect = [值], BootSE = [值]
    95% BootCI [下限, 上限] → [不含 0: 中介效应显著 / 含 0: 不显著]

(5) 直接效应 (c'):
    B = [值], SE = [值], p = [值] → [显著/不显著]

结论: [中介变量] 发挥了 [完全中介 / 部分中介 / 无中介] 作用。
```

---

## 调节效应解读

```
层次回归结果:
- Step 1 (主效应): R² = [值]
- Step 2 (交互项): R² = [值], ΔR² = [值], ΔF = [值], p = [值]
- 交互项 β = [值], t = [值], p = [值] → [显著/不显著]

[交互项显著时] 简单斜率分析:
- [调节变量] 高 (+1SD): [自变量] → [因变量], β = [值], p = [值]
- [调节变量] 低 (-1SD): [自变量] → [因变量], β = [值], p = [值]

结论: [调节变量] [显著/不显著] 调节了 [自变量] 与 [因变量] 的关系。
在 [调节变量] 水平高时，[自变量] 的效应 [更强/更弱/方向相反]。
```
